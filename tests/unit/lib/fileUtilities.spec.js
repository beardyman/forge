
/*
describe( 'File Utilities', function() {
  beforeEach( function() {

    esmock();

  });

  describe( 'Windows', function() {
    let currentPlatform;
    before( function() {
      currentPlatform = process.platform;
      Object.defineProperty( process, 'platform', {
        value: 'win32'
      });
    });

    after( function() {
      Object.defineProperty( process, 'platform', {
        value: currentPlatform
      });
    });

    it( 'should run a mock migration on a windows machine ', async function() {

    });
  });
});
*/
